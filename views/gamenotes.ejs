<%- include('parts/header.ejs') %>

<main class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-10">
      <h1 class="text-warning mb-4 fw-bold text-center">
        <i class="fas fa-trophy me-2"></i> Мои Игры
      </h1>

      <div class="text-end mb-3">
        <a href="/logout" class="btn btn-outline-danger">
          <i class="fas fa-sign-out-alt me-1"></i> Выйти
        </a>
      </div>

      <!-- Добавление новой игры -->
      <div class="card shadow-lg mb-4 bg-dark text-white border-primary">
        <div class="card-body p-4">
          <h5 class="card-title text-warning">
            <i class="fas fa-plus-circle me-2"></i> Добавить новую игру
          </h5>

          <form action="/add-game" method="POST" class="row g-3 align-items-center">
            <div class="col-md-5">
              <input type="text" class="form-control" name="gameName"
                     placeholder="Название игры" required>
            </div>

            <div class="col-md-3">
              <select class="form-select" name="gameStatus" required>
                <option value="" disabled hidden>Выберите статус...</option>
                <option value="completed">✅ Пройдено</option>
                <option value="playing">🎮 Играю</option>
                <option value="planned">🗓️ Планирую</option>
                <option value="abandoned">👎 Бросил</option>
              </select>
            </div>

            <div class="col-md-2">
              <input type="number" class="form-control" name="gameRating"
                     placeholder="Оценка (1-10)" min="1" max="10">
            </div>

            <div class="col-12">
              <textarea class="form-control" name="gameComment" rows="2"
                        placeholder="Комментарий..."></textarea>
            </div>

            <div class="col-12 d-grid">
              <button type="submit" class="btn btn-primary btn-lg">
                <i class="fas fa-plus me-1"></i> Добавить игру
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Таблица игр -->
      <div class="card shadow-lg bg-light">
        <div class="card-body p-0">
          <% const completed = games.filter(g => g.status === 'completed'); %>
          <% const playing = games.filter(g => g.status === 'playing'); %>
          <% const planned = games.filter(g => g.status === 'planned'); %>
          <% const abandoned = games.filter(g => g.status === 'abandoned'); %>

          <% const renderList = (title, color, arr) => { %>
            <h4 class="p-3 text-<%= color %> fw-bold"><%= title %></h4>
            <% if (arr.length > 0) { %>
              <div class="table-responsive mb-4">
                <table class="table table-striped table-hover align-middle">
                  <thead class="table-dark">
                    <tr>
                      <th>Название</th>
                      <th>Оценка</th>
                      <th>Комментарий</th>
                      <th class="text-end">Действия</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% arr.forEach(g => { %>
                      <tr>
                        <td><%= g.game_name %></td>
                        <td>
                          <% if (g.rating !== null && g.rating !== undefined) { %>
                            <span class="badge bg-success fs-6"><%= g.rating %>/10</span>
                          <% } else { %>
                            <span class="text-muted">N/A</span>
                          <% } %>
                        </td>
                        <td><%= g.comment ? g.comment : '-' %></td>
                        <td class="text-end">
                          <form action="/delete-game" method="POST" class="d-inline">
                            <input type="hidden" name="gameId" value="<%= g.id %>">
                            <button type="submit" class="btn btn-sm btn-outline-danger">
                              <i class="fas fa-trash-alt me-1"></i> Удалить
                            </button>
                          </form>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <p class="text-muted text-center pb-3">Нет игр со статусом "<%= title %>".</p>
            <% } %>
          <% } %>

          <% renderList("✅ Пройдено", "success", completed); %>
          <% renderList("🎮 Играю", "info", playing); %>
          <% renderList("🗓️ Планирую", "warning", planned); %>
          <% renderList("👎 Бросил", "danger", abandoned); %>
        </div>
      </div>
    </div>
  </div>
</main>

<%- include('parts/footer.ejs') %>
