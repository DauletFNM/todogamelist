<%- include('parts/header.ejs') %>

<main class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-10">
      <h1 class="text-warning mb-4 fw-bold text-center">
        <i class="fas fa-trophy me-2"></i> Мои Игры
      </h1>

      <!-- Кнопка выхода -->
      <div class="text-end mb-3">
        <a href="/logout" class="btn btn-outline-danger">
          <i class="fas fa-sign-out-alt me-1"></i> Выйти
        </a>
      </div>

      <!-- Добавление новой игры -->
      <div class="card shadow-lg mb-4 bg-dark text-white border-primary">
        <div class="card-body p-4">
          <h5 class="card-title text-warning">
            <i class="fas fa-plus-circle me-2"></i> Добавить новую игру
          </h5>

          <form action="/add-game" method="POST" class="row g-3 align-items-center">
            <div class="col-md-5">
              <input type="text" class="form-control" id="gameName" name="gameName"
                     placeholder="Название игры" required>
            </div>

            <div class="col-md-3">
              <select class="form-select" id="gameStatus" name="gameStatus" required>
                <option value="" disabled hidden>Выберите статус...</option>
                <option value="completed">✅ Пройдено</option>
                <option value="playing">🎮 Играю</option>
                <option value="planned">🗓️ Планирую</option>
                <option value="abandoned">👎 Бросил</option>
              </select>
            </div>

            <div class="col-md-2">
              <input type="number" class="form-control" id="gameRating" name="gameRating"
                     placeholder="Оценка (1-10)" min="1" max="10">
            </div>

            <div class="col-12">
              <textarea class="form-control" id="gameComment" name="gameComment"
                        rows="2" placeholder="Комментарий..."></textarea>
            </div>

            <div class="col-12 d-grid">
              <button type="submit" class="btn btn-primary btn-lg">
                <i class="fas fa-plus me-1"></i> Добавить игру
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Таблица игр -->
      <div class="card shadow-lg bg-light">
        <div class="card-body p-0">
          <ul class="nav nav-tabs nav-fill" id="gameTabs" role="tablist">
            <li class="nav-item" role="presentation">
              <button class="nav-link active" id="completed-tab" data-bs-toggle="tab"
                      data-bs-target="#completed" type="button" role="tab">✅ Пройдено</button>
            </li>
            <li class="nav-item" role="presentation">
              <button class="nav-link" id="playing-tab" data-bs-toggle="tab"
                      data-bs-target="#playing" type="button" role="tab">🎮 Играю</button>
            </li>
            <li class="nav-item" role="presentation">
              <button class="nav-link" id="planned-tab" data-bs-toggle="tab"
                      data-bs-target="#planned" type="button" role="tab">🗓️ Планирую</button>
            </li>
            <li class="nav-item" role="presentation">
              <button class="nav-link" id="abandoned-tab" data-bs-toggle="tab"
                      data-bs-target="#abandoned" type="button" role="tab">👎 Брошено</button>
            </li>
          </ul>

          <div class="tab-content p-3">
            <% 
              const completedGames = games.filter(g => g.status === 'completed');
              const playingGames = games.filter(g => g.status === 'playing');
              const plannedGames = games.filter(g => g.status === 'planned');
              const abandonedGames = games.filter(g => g.status === 'abandoned');
            %>

            <% const renderTable = (gameList, label) => { %>
              <% if (gameList.length > 0) { %>
                <div class="table-responsive">
                  <table class="table table-striped table-hover align-middle">
                    <thead class="table-dark">
                      <tr>
                        <th>Название</th>
                        <th>Оценка</th>
                        <th>Комментарий</th>
                        <th class="text-end">Действия</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% gameList.forEach(game => { %>
                        <tr>
                          <td><%= game.game_name %></td>
                          <td>
                            <% if (game.rating !== null && game.rating !== undefined) { %>
                              <span class="badge bg-success fs-6"><%= game.rating %>/10</span>
                            <% } else { %>
                              <span class="text-muted">N/A</span>
                            <% } %>
                          </td>
                          <td><%= game.comment ? game.comment : '-' %></td>
                          <td class="text-end">
                            <form action="/delete-game" method="POST" class="d-inline">
                              <input type="hidden" name="gameId" value="<%= game.id %>">
                              <button type="submit" class="btn btn-sm btn-outline-danger" title="Удалить">
                                <i class="fas fa-trash-alt me-1"></i> Удалить
                              </button>
                            </form>
                          </td>
                        </tr>
                      <% }) %>
                    </tbody>
                  </table>
                </div>
              <% } else { %>
                <p class="text-muted text-center pt-3">Нет игр со статусом "<%= label %>".</p>
              <% } %>
            <% } %>

            <div class="tab-pane fade show active" id="completed" role="tabpanel">
              <% renderTable(completedGames, "Пройдено") %>
            </div>

            <div class="tab-pane fade" id="playing" role="tabpanel">
              <% renderTable(playingGames, "Играю") %>
            </div>

            <div class="tab-pane fade" id="planned" role="tabpanel">
              <% renderTable(plannedGames, "Планирую") %>
            </div>

            <div class="tab-pane fade" id="abandoned" role="tabpanel">
              <% renderTable(abandonedGames, "Брошено") %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>

<%- include('parts/footer.ejs') %>
